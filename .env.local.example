# TMDB v3 API key (32-char hex) — used as ?api_key= query param
TMDB_API_KEY=your_v3_api_key_here

# TMDB v4 read access token (JWT, starts with eyJ...) — preferred, used as Bearer token
API_READ_ACCESS_TOKEN=your_read_access_token_here
